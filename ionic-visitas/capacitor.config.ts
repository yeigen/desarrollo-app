import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mediclinic.visitas',
  appName: 'MediClinic Visitas',
  webDir: 'dist'
};

export default config;
